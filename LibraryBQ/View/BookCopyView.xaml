<Window x:Class="LibraryBQ.View.BookCopyView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:LibraryBQ.View"
        mc:Ignorable="d"
        Title="BookCopyView" Height="300" Width="450">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="2*"/>
            <RowDefinition Height="3*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="10" Orientation="Vertical">
            <TextBlock x:Name="QueriedBookTitle" Margin="0,3" FontSize="18" FontWeight="SemiBold" Foreground="DarkBlue" VerticalAlignment="Center"/>
            <TextBlock x:Name="QueriedBookAuthor" Margin="0,1" FontSize="15" VerticalAlignment="Center"/>
            <TextBlock x:Name="QueriedBookNum" Margin="0,1" FontSize="15" VerticalAlignment="Center"/>
        </StackPanel>

        <DataGrid x:Name="gridBookCopy" Grid.Row="1" AutoGenerateColumns="False" IsReadOnly="False" SelectionMode="Single" FontSize="12">
            <DataGrid.ColumnHeaderStyle>
                <Style TargetType="DataGridColumnHeader">
                    <Setter Property="Foreground" Value="Gray"/>
                    <Setter Property="FontSize" Value="15"/>
                    <Setter Property="FontWeight" Value="SemiBold"/>
                </Style>
            </DataGrid.ColumnHeaderStyle>
            <DataGrid.Columns>
                <DataGridTextColumn Header="Id" Binding="{Binding}" Width="*" Visibility="Hidden"/>
                <DataGridTextColumn Header="등록번호" Binding="{Binding}" Width="*"/>
                <DataGridTextColumn Header="도서상태" Binding="{Binding}" Width="*"/>
                <DataGridTextColumn Header="반납예정일" Binding="{Binding}" Width="*"/>
                <DataGridTextColumn Header="대출/예약" Binding="{Binding}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Window>
